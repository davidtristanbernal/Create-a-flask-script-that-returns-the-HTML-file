# Create-a-flask-script-that-returns-the-HTML-file

Create a flask script that returns the HTML file, message.html which is styled using the stylesheet 
style.css. The flask template system finds the static CSS file under static/css directory. Hence the 
style.css is saved at the specified path.
